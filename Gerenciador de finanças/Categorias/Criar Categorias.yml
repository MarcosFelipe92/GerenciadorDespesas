info:
  name: Criar Categorias
  type: http
  seq: 2

http:
  method: POST
  url: http://{{host}}:{{port}}/categorias/
  body:
    type: json
    data: |-
      [
        {
          "descricao": "Lazer"
        }
      ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
