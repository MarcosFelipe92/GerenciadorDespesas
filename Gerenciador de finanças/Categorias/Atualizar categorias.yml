info:
  name: Atualizar categorias
  type: http
  seq: 3

http:
  method: PUT
  url: http://{{host}}:{{port}}/categorias/5
  body:
    type: json
    data: |-
      {
        "descricao": "Transporte"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
